[project]
name = "rag-core"
version = "0.1.0"
description = "Shared RAG primitives for the Azure RAG platform API and worker applications."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_core"]
